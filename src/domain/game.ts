export type Game = {
  id: string;
  title: string;
  coverImageUrl: string;
  description: string;
  year: number;
  gender: string;
  imdbScore: number;
  trailerYouTubeUrl: string;
  gameplayYouTubeUrl: string;
  createdAt: string;
  updatedAt: string;
  userId: string;
  profileId: string;
};
